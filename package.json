{
  "name": "app",
  "private": true,
  "version": "1.1.2",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && tsc -p electron/tsconfig.json && electron-builder",
    "preview": "vite preview",
    "electron:dev": "concurrently -k \"cross-env BROWSER=none npm run dev\" \"wait-on tcp:3000 && tsc -p electron/tsconfig.json && electron .\"",
    "electron:build": "tsc && vite build && tsc -p electron/tsconfig.json && electron-builder"
  },
  "build": {
    "appId": "com.lovechurch.broadcast-helper",
    "productName": "사랑의빛교회 방송용 툴",
    "files": [
      "dist/**/*",
      "dist-electron/**/*"
    ],
    "directories": {
      "output": "release"
    },
    "artifactName": "love-light-broadcast-tool-setup-${version}.${ext}",
    "win": {
      "target": "nsis",
      "icon": "public/icon.png"
    },
    "publish": {
      "provider": "github",
      "owner": "scrafts",
      "repo": "love-light-broadcast-tool"
    },
    "forceCodeSigning": false
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "es-hangul": "^2.3.3",
    "react": "^19.2.1",
    "react-dom": "^19.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "electron": "^40.0.0",
    "electron-builder": "^26.4.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0",
    "wait-on": "^9.0.3"
  }
}
